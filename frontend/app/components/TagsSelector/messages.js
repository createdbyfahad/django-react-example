/*
 * TagsSelector Messages
 *
 * This contains all the text for the TagsSelector component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.TagsSelector.header',
    defaultMessage: 'This is the TagsSelector component !',
  },
});
